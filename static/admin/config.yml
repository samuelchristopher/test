backend:
  name: git-gateway
  branch: master

media_folder: static/assets
public_folder: assets

collections:
  - name: beverage
    label: Beverage
    folder: beverage
    create: true
    fields:
      - { name: title, label: Title }
      - { name: image, label: Image, widget: image }
      - { name: desc, label: Desc, widget: markdown }
      - { label: "Small Price", name: sprice }
      - { label: "Regular Price", name: rprice }